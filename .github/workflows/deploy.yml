name: Deploy to Azure

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to deploy to'
        required: true
        default: 'dev'
        type: choice
        options:
          - dev
          - staging
          - prod

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment || 'dev' }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js 20.x
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build TypeScript
        run: npm run build

      - name: Run tests
        run: npm test -- unit

      - name: Deploy to Azure Functions
        uses: Azure/functions-action@v1
        with:
          app-name: func-eshf-embeddings-${{ github.event.inputs.environment || 'dev' }}
          package: .
          respect-funcignore: true
          publish-profile: ${{ secrets.AZURE_FUNCTIONAPP_PUBLISH_PROFILE }}

      - name: Test deployment
        run: |
          FUNC_URL="https://func-eshf-embeddings-${{ github.event.inputs.environment || 'dev' }}.azurewebsites.net"
          echo "Testing function app at $FUNC_URL"

          # Wait for function app to be ready
          sleep 30

          # Test status endpoint
          STATUS_CODE=$(curl -s -o /dev/null -w "%{http_code}" "$FUNC_URL/api/status" || echo "000")
          echo "Status endpoint returned: $STATUS_CODE"

          if [ "$STATUS_CODE" = "200" ] || [ "$STATUS_CODE" = "401" ]; then
            echo "✅ Deployment successful - Function app is responding"
          else
            echo "⚠️  Function app may still be starting up (Status: $STATUS_CODE)"
          fi

      - name: Deployment summary
        run: |
          echo "### Deployment Summary 🚀" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Environment:** ${{ github.event.inputs.environment || 'dev' }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Function App:** func-eshf-embeddings-${{ github.event.inputs.environment || 'dev' }}" >> $GITHUB_STEP_SUMMARY
          echo "- **URL:** https://func-eshf-embeddings-${{ github.event.inputs.environment || 'dev' }}.azurewebsites.net" >> $GITHUB_STEP_SUMMARY
          echo "- **Deployed by:** ${{ github.actor }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Commit:** ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
